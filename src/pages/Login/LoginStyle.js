import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles({
  containerStyle: {
    marginTop: '7em',
    margin: 'auto',
    width: '40%',
    padding: '3em'
  },
  infoPanel: {
    marginTop: '3em',
    textAlign: 'center'
  }
});

export default useStyles;
